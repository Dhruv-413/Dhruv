# DESIGN.md — design contract for dhruv-portfolio

> The `design-critic` agent and `/design-review` score against this file. Read it before changing any visual.
> **Status: DECIDED 2026-09-20** (direction chosen by Claude on the owner's instruction "make the decision yourself"; see §5).

## 1. The decision in one paragraph
**INK / BONE — "Signal".** A Swiss-print × terminal-telemetry system built from four materials the owner actually has: **type, a strict grid,
real GitHub data, and one personal image — the pixel-art avatar.** Dark theme = *telemetry* (warm ink, phosphor-bone text), light theme = *print*
(bone paper, carbon ink); same grid, same type, same single accent (hazard vermilion). Zero border-radius, zero shadows/gradients/glass; structure comes from
1 px hairlines, crosshair markers and extreme type-scale contrast. **Signature moment: the Pixel Portrait** — the avatar rebuilt as a live cell mosaic that
"decodes" on load and heats up under the cursor; the same square cell is the visual unit of the GitHub heatmap and of page transitions.
Direction skill used: `industrial-brutalist-ui` (one archetype per substrate; see §4.10 for where we deliberately deviate).

## 2. Why this direction (evidence, not taste)
- **No visual assets exist.** `public/` has no images; all 5 project images (`/images/projects/*.jpg`), `og-image.jpg` and `twitter-image.jpg` are referenced but missing.
  Every Codrops/Behance portfolio researched (R—K ’26, Rocca, Reymondin, Bisous) leans on photography/CGI. This site can't, so the visual must be **type + grid + data + code**.
- **The pixel-art avatar is the owner's only personal image**, and GitHub's contribution graph is literally a grid of pixels — one motif ties identity, data and code together.
- **Convergent signal from the case studies:** one strict grid (12 col / 4 col), 1–2 typefaces + a mono "technical" layer (Bisous, Reymondin), restrained palette with a single accent,
  terminal/decode text as the "geeky dev side" (Reymondin), motion as rhythm not noise (R—K: "built for presence, not volume"), reduced-motion fallbacks (Rocca).
- **Real data is modest** (1 star, 0-day streak, 548 contributions, 9 repos) → stats must never be headlines; data is *texture*, not claims.

### What we are fixing (slop tells seen on the live site, dhruvgupta-nu.vercel.app)
Everything centred with no grid tension · `via-purple-500` glow behind the avatar · macOS-window code snippet as hero furniture · "Hi, I'm" + filler copy ("Building the Future",
"production-ready solutions with modern web technologies") · four identical stat cards on every page ("100% Production", "1 Star") · glassmorphism/blur/gradient text · Inter default · empty image slots.

## 3. Non-negotiables
- Lighthouse mobile ≥ 95 every route; LCP ≤ 2.0 s (LCP element = the display name, never the canvas); CLS ≤ 0.05; INP ≤ 150 ms.
- WCAG 2.2 AA in **both** themes (values in §4.1 are computed, not guessed); visible `focus-visible`; full keyboard use; `prefers-reduced-motion` honoured everywhere.
- Canvas/WebGL is lazy, capped (DPR ≤ 2), paused off-screen and when the tab is hidden, and degrades to a static frame under reduced motion / no JS.
- Tokens only; no hex/rgb or named palette colours (`purple-500`…) in components. Real content only; no invented facts or metrics.
- Zero new dependencies for the first slice (CSS + canvas 2D + `next/font`).

## 4. Decisions (literal values — the critic scores against these)

### 4.1 Colour (oklch; contrast computed with WCAG 2.x, all pass)
| Token | Dark — "telemetry" | Light — "print" |
|---|---|---|
| `--background` | `oklch(0.145 0.006 60)` ≈ #0c0a08 | `oklch(0.955 0.009 90)` ≈ #f2f0e9 |
| `--foreground` | `oklch(0.945 0.008 85)` ≈ #efece7 (16.9:1) | `oklch(0.155 0.006 60)` ≈ #0e0c0a (17.2:1) |
| `--card` / `--popover` | `oklch(0.18 0.006 60)` | `oklch(0.975 0.007 90)` |
| `--muted` / `--secondary` / `--accent-surface` | `oklch(0.225 0.006 60)` | `oklch(0.925 0.01 90)` |
| `--muted-foreground` | `oklch(0.70 0.01 80)` (7.4:1) | `oklch(0.45 0.01 70)` (6.5:1) |
| `--primary` (the ONE accent, "signal") | `oklch(0.68 0.21 35)` ≈ #ff582d (6.3:1 on bg) | `oklch(0.55 0.225 30)` ≈ #d60400 (4.8:1 on bg) |
| `--primary-foreground` | `oklch(0.145 0.006 60)` (6.3:1 on accent) | `oklch(0.975 0.007 90)` (5.1:1 on accent) |
| `--border` (hairline) | `oklch(0.945 0.008 85 / 16%)` | `oklch(0.155 0.006 60 / 20%)` |
| `--input` (control edge, ≥3:1) | `oklch(0.945 0.008 85 / 42%)` (≈3.7:1; the original 34% measured ≈2.7:1 and failed) | `oklch(0.155 0.006 60 / 75%)` (measured ≈8.4:1) |
| `--ring` | = `--primary` | = `--primary` |

Rules: accent used for ≤ ~5% of any viewport (one word, one rule, one live cell set, one CTA). No second accent. `--chart-1..5` become an accent-derived ramp (single hue, lightness steps). Green is not used.

### 4.2 Type (all via `next/font/google`, variable)
- **Display + body: Bricolage Grotesque** (axes `opsz`, `wdth`, `wght`). Display: weight 800, `wdth` 84, uppercase, tracking −0.028em, leading 0.84 (the original `wdth` 80 / −0.045em made letters collide; built and measured). H1: `wdth` 92, tracking −0.005em. H2 and list titles: `wdth` 96, tracking +0.005em. Body: weight 400, `wdth` 100, opsz auto.
- **Mono (labels, nav, data, code): JetBrains Mono** (already in the project). Labels uppercase, tracking 0.08em.
- **Editorial accent: Instrument Serif Italic** — max one emphasised word per heading, never in body copy.
- Retire Inter and the misleading `--font-geist-*` variable names → `--font-display`, `--font-serif`, `--font-mono`.
| Role | Size | Leading | Notes |
|---|---|---|---|
| Display | `clamp(4rem, 18vw, 17rem)` (mobile 26vw) | 0.84 | uppercase, viewport-bleeding, name only |
| H1 | `clamp(2.75rem, 7vw, 6.5rem)` | 0.9 | uppercase |
| H2 | `clamp(2rem, 4vw, 3.75rem)` | 0.95 | |
| H3 | `1.5rem` | 1.15 | |
| Body | `1.0625rem` | 1.6 | measure ≤ 62ch |
| Mono label | `0.75rem` | 1.3 | uppercase, tracking 0.08em |

### 4.3 Grid & spacing
12-column grid; page margin `clamp(1rem, 3.5vw, 3rem)`; gutter `clamp(1rem, 2vw, 2rem)`; content max 1600 px, hero/marquees full-bleed. 4-px base; scale 4·8·12·16·24·32·48·64·96·128·192.
Section block padding `clamp(4rem, 9vw, 8rem)` (was `clamp(5rem, 12vw, 11rem)`; the design review read ~170 px bands as dead space). "Spec-sheet" panels use `display:grid; gap:1px` on a `--border` background so cells get razor-thin dividers.
Every section carries a mono index `[01]`, and grid intersections may carry a `+` crosshair (decorative, `aria-hidden`).

### 4.4 Shape, depth, texture
`--radius: 0` everywhere (dot indicators may be round). No box-shadow, no blur/backdrop-filter, no gradients (except the functional pixel-heat ramp). Depth = tone shift + hairline.
Emphasis border 2 px `--foreground`. Focus ring: 2 px `--primary`, 2 px offset. Optional texture: static grain overlay ≤ 5% opacity (single fixed layer, no animation).

### 4.5 Motion
Durations: 120 ms (micro) · 220 ms (UI) · 420 ms (reveal) · 700 ms (section) · ≤ 1400 ms (hero intro). Easing: out `cubic-bezier(0.16,1,0.3,1)`, in-out `cubic-bezier(0.83,0,0.17,1)`.
Stagger 35 ms. Animate `transform`, `opacity`, `clip-path` only. Scroll reveals use CSS `animation-timeline: view()` inside `@supports`, no-op otherwise.
Text effect vocabulary (mono labels only): **decode** (scramble → resolve, 400–600 ms, once) and **typed cursor** (block `▌`). Reduced motion: no movement, no scramble, final states rendered immediately.

### 4.6 Signature: Pixel Vortex (hero; the Pixel Portrait, made interactive)
The owner's 64×64 GitHub avatar (`/images/portrait.png`, self-hosted → no CORS/API dependency) is 4096 flat tiles drawn as instanced quads: colour = the pixel, no height map. **Raw WebGL2, one instanced draw call, no 3D library, no MSAA, no lighting** (lazy chunk ≈ 5 kB; framer-motion is no longer used at runtime). All motion is analytic in the vertex shader, so a frame costs one small uniform upload and 16k vertices, which an old integrated GPU handles; there is no per-tile CPU physics.
Load: every tile spirals in from a wider, rotated orbit and lands on its pixel, flashing `--primary` (≈ 1.7 s). Pointer: a whirlpool follows the cursor and leaves a short wake; nearby tiles twist, lift and tint, then unwind to the picture in ≈ 1 s (faster strokes swirl harder); the picture also tilts slightly with the pointer. Click / tap: a ring pulse shoves tiles outward as it crosses the face. Scroll: leaving the hero winds the picture slowly around its centre. Once, right after the intro, one scripted swirl crosses the face so the interaction is discoverable (cancelled by the first pointer input). The portrait always settles back to the clean, frontal picture, and the loop **rests** when nothing moves (WCAG 2.2.2, no idle battery cost).
Budget: canvas ≤ 720 px per side and DPR ≤ 1.5; devices with ≤ 4 cores or ≤ 4 GB start at 0.72 quality; if the averaged frame time exceeds 34 ms the quality steps down (0.72 → 0.5) and then animation stops (static frame); pauses off-screen and when the tab is hidden. Init is deferred until the page has painted and gone idle; on touch devices it arms on the first touch (6 s fallback).
Fallbacks, cheapest first: server-rendered pixelated `<img>` (accessible name "Pixel-art portrait of Dhruv Gupta", works with JS off) → 2D canvas mosaic when WebGL2 is unavailable / context lost → reduced motion = one static render, scroll-linked motion also off. Text is never rendered inside the canvas.
Same cell language reused for: GitHub heatmap, page-transition wipe, loading skeletons (later slices).

### 4.7 Copy voice
First person, terse, specific, present tense. Labels in uppercase mono. **Banned:** "Hi, I'm", passionate, innovative, cutting-edge, production-ready, "Building the Future", emoji, vanity stats (stars, "100%"). Numbers only if true and meaningful (e.g. "87% query accuracy").

### 4.8 Components
Buttons: rectangular, 2 px border, mono uppercase label + arrow (`→ ↓ ↗`); primary = accent fill. Nav: mono, numbered, bottom-aligned underline for active. Cards: no card look — bordered cells in a hairline grid.
Project "covers" (no screenshots exist): typographic — oversized index numeral, category tag, generative pixel pattern seeded from `id`, tech in mono. Replace any image slot that has no asset.

### 4.9 Information architecture (rollout order)
1. **Slice 1 (BUILT 2026-09-20, uncommitted; PERF GATE NOT MET on `/`: Lighthouse mobile 77-88 vs the ≥ 95 in §3, CLS 0 but LCP ≈ 3.8 s simulated; see `docs/DESIGN-APPROACH.md` §3.5):** tokens + fonts + Header + Hero (Voxel Portrait) + a server-rendered work index on `/` (interim for slice 2) + token-only footer. Built on the earlier prototype patch; verified with tsc, eslint, `next build`,
   browser checks at 375/768/1280/1440/1920 in both themes, reduced-motion, keyboard, and three independent reviews (design, a11y/motion, performance). Open items: mobile Lighthouse (see `docs/DESIGN-APPROACH.md` §3.5).
2. **Slice 2 (BUILT 2026-09-20):** Work index (hairline list, category filter island) → case-study pages `/projects/[id]` (static params, JSON-LD, seeded typographic cover, per-project OG image, prev/next); sitemap lists the real project URLs.
3. **Slice 3 (BUILT 2026-09-20; Career rebuilt 2026-09-21, see §5):** Career as a chronological ledger; Skills as a hairline matrix + certifications list; Contact form restyled in place (behaviour unchanged). There is no separate About route in the IA; the home hero carries the facts.
4. **Slice 4 (BUILT 2026-09-20; GitHub rebuilt 2026-09-21, see §5):** `/github` heatmap in the cell language (all widths, scrolls on phones) with loading/error/empty states; footer; route-enter transition (`template.tsx`, transform only); 404 and all `loading.tsx` states.

### 4.10 Deliberate deviations from `industrial-brutalist-ui`
Accent is hazard **vermilion/orange-red** (`≈#ff582d` dark, `≈#d60400` light) instead of a single `#E61919` — warmer to match the avatar's palette while keeping the skill's one-accent rule.
Two substrates (dark telemetry / light print) are both supported because the site has a theme toggle; each is internally consistent and they are never mixed on one screen.
Bricolage Grotesque replaces Inter/Archivo for the macro face (more character, variable width axis).

## 5. Decision log
| Date | Decision | Why |
|------|----------|-----|
| 2026-09-20 | Direction = INK / BONE "Signal" (Swiss-print × terminal), signature = Pixel Portrait | §2: no assets → type/grid/data; avatar is pixel art; GitHub grid is pixels; case-study convergence |
| 2026-09-20 | Palette, type, scale, motion values in §4 | Contrast computed (scratch script); fonts chosen for character + variable axes; zero-dependency first slice |
| 2026-09-20 | Retire Inter, purple glow, glass, gradients, stat-card rows, mac code window as hero | Slop tells listed in §2 |
| 2026-09-20 | Mobbin not usable (paid plan) — inspiration via Codrops/Behance case studies (Exa) | Tool limitation |
| 2026-09-20 | Hero signature extended from a 2D mosaic to a 3D voxel relief, raw WebGL2, no new dependency | Owner opened the brief to 3D on the build request; raw WebGL2 keeps the cost at ≈ 7.6 kB vs ≈ 150 kB for three.js; measured 154 fps idle / 109 fps at 4× CPU throttle |
| 2026-09-20 | Idle motion rests after ≈ 5 s; scroll-linked tilt also honours reduced motion | a11y review: WCAG 2.2.2 (auto-updating > 5 s) and a reduced-motion leak; matches §4.6 "idle: static" |
| 2026-09-20 | Display `wdth` 80→84, tracking −0.045→−0.028em; H1/H2 tracking loosened; display size 15vw→18vw | Built and screenshotted: letters collided; the larger size fills the columns and removes hero dead space |
| 2026-09-20 | Dark `--input` alpha 34%→42%; §4.1 contrast claim corrected | Computed contrast was ≈ 2.7:1, not the documented 6.4:1; UI edges need ≥ 3:1 |
| 2026-09-20 | `--section-pad` `clamp(5rem,12vw,11rem)` → `clamp(4rem,9vw,8rem)` | Design review: the ~170 px bands above/below sections read as dead space, not cinematic pacing |
| 2026-09-20 | Slices 2-4 built in one pass on the owner's request ("make the entire site look good"); every legacy client component removed (framer-motion `motion.*`, AnimatedBackground, StatCard, SectionHeader, project modal, etc.) plus the root `MotionConfig` | The legacy pages carried the slop tells and ~179 hard-coded palette classes; framer-motion now only remains inside the lazy 3D chunk (`useScroll`) |
| 2026-09-20 | Touch devices arm the 3D portrait on first touch/scroll (6 s fallback); desktop arms on idle | Mobile Lighthouse TBT on `/` fell from ~380 ms to ~130 ms; touch has no cursor for the heat effect |
| 2026-09-20 | No proficiency numbers or bars on Skills; project pages show `metrics` only, not `badges` | Self-assessed percentages are vanity data (§4.7); badges duplicated the metrics and included banned copy ("Production Ready") |
| 2026-09-20 | Footer restyled (hairline, mono, server component) ahead of slice 4; floating scroll-to-top removed | The old footer carried the purple gradient / rounded chip slop tells and a scroll listener |
| 2026-09-20 | Hero copy uses only facts already in SITE_CONFIG / timeline.json; no availability claim | Owner has not confirmed "available"; the Deloitte/ONGC internship conflict is unresolved |
| 2026-09-20 | Home below-the-fold: server-rendered work index replaces the legacy client sections | Removes `ssr:false`, the filler "Core Values" and the code-window furniture; the old components were deleted |
| 2026-09-20 | Home below-the-fold is now an editorial About (not a project list): sentence-case `.t-statement` whose words light up on scroll, a 7-block week strip (5 Deloitte / 2 Beaumonde, today marked in IST by a tiny client island), two plain-language lane blocks, a "before this" readout row, and doorways to Projects and Contact | Owner feedback: the site read as one texture (hairline lists everywhere) and the About page carried a project list. The About is the one section that is not a list. Statement is sentence case for readability; display stays uppercase |
| 2026-09-20 | Slow CSS ticker (`.marquee`) between hero and About; pauses on hover/focus, stops under reduced motion, hidden from AT | Adds motion and life without JS; every phrase is already stated on the site |
| 2026-09-20 | Current roles are stated by the owner and live in `SITE_CONFIG.person.currentRole` / `.venture`: Data Modernization and Migration Intern at Deloitte; weekend contributor to Beaumonde (family B2B sourcing). Deloitte started 2026-01-21 as a virtual SAP Analyst intern (to June 2026); from July 2026 the owner is onsite and the role is Data Modernization and Migration Intern (amended 2026-09-21, see the Career row); Beaumonde stays off the career page (owner's choice, 2026-09-21) | Supersedes the 2026-09-20 hero-copy row about the unresolved Deloitte/ONGC conflict (ONGC is the 2025 internship; Deloitte is current) |
| 2026-09-20 | Voxel portrait made legible: near-frontal rest pose (0.08 / 0.09 rad), relief 6.5 to 2.4, softer cursor parallax, brighter ambient light, heat tints instead of repainting | Owner feedback: "a little hard to get the correct image". Height-from-luminance at 6.5 turned the face into a mask and the 18 degree tilt sheared it; §4.6 already said relief stays low so the face reads |
| 2026-09-20 | Voxel relief replaced by the flat-tile Pixel Vortex: instanced quads (no cubes, depth map, MSAA or lighting), analytic whirlpool trail + click ring + spiral-in intro, one scripted demo swirl, quality steps for weak devices, framer-motion dropped from the hero | Owner: "make the pixel vortex not lag on low-end PC, interesting and fun to interact with". Fill-rate (2x DPR, MSAA, 5-face cubes) was the low-end risk; a flat 720 px canvas removes it, and the swirl reads as play rather than as a distortion of the face |
| 2026-09-21 | About statement gets a "Fig. 02" beside it on `lg`+: two 8x8 tile grids (old / new system) with the same seeded pattern, an E / T / L stage column between them, and a "Moved n / total" counter. As the statement is read, a copy of each tile travels from its old-grid position to its new-grid one (vermilion in transit, sized with container units), the stages light in turn and the counter ticks up (all CSS scroll-driven with an integer `@property`; finished state by default, static under reduced motion, aria-hidden, not rendered below `lg`) | Owner: empty space right of the statement, then "make it interesting". The sentence is about moving data between systems, so the figure shows the move (extract, transform, load) rather than a fade, in the cell language of §4.6 |
| 2026-09-21 | Projects: the seeded dot-pattern cover is removed. Each project gets its own small illustration about what it does, in a square frame: EcoHive = honeycomb of cells (hover leaves a fading trail; legend trees / water / air / mangroves), Eye Gaze = an eye that follows the cursor with a reticle and gaze readout (click blinks), Stock Analysis = one agent, five agents, a data-source ticker tape and candles (wires draw on scroll, hover sends packets), MUJ Placement Portal = manual sheets to one database (toggle, plays once into "after"), Crave Connect = a dish map where the five dishes nearest the cursor light up (semantic search, illustrated). The index shows the hovered row's art in the sticky panel; the case study puts it beside the title. Motion is hover-, scroll- or input-driven, never an idle loop; only the eye, the map and the placement toggle are client islands (a few kB). Case study also shows numbered plain-language chapters, a facts strip, a build rail (which of the five) and a "Private repository" note instead of links and code for private projects | Owner supplied each project's real story, corrected the facts (Crave Connect dropped, both worked on the backend; Eye Gaze team of 3; Stock Analysis solo with untrue numbers; MUJ portal timeline Jun 2025 to 2026) and asked for the dot pattern to go and each project to have its own design. Facts were checked against the public repositories: Stock Analysis data sources, agents and the untrue accuracy / speed numbers were replaced with what the README and source show (5 agents, 5 data sources); Crave Connect shows its real 19-table schema; EcoHive gains its live link; code excerpts are now real files from the repos |
| 2026-09-21 | Skills rebuilt as one bento plus a certificates ledger (`/skills`). (1) **Stack bento**: 12 columns from lg, tiled so every row sums to 12 (large tiles 6 + 6, then 3 + 5 + 4, sized so all three wrap their chips to two rows, and 4 + 4 + 4), 2 columns from md, 1 below; hairline cells, every cell covered (an empty cell would show as a solid border-colour block, so coverage is asserted per breakpoint in the browser check). The two newest groups (Data Engineering, Cloud; learned at Deloitte) are the large tiles and each carries a small figure that the skill chips drive (frames drawn in hairline foreground; the accent is only on moving or selected elements): Data Lake / Data Warehouse / Lakehouse / Apache Spark move 24 records between four arrangements, Azure VM Scale Sets / Functions / Storage dim the other two lanes of a Traffic slider that adds machines, runs functions only while triggered and fills storage; each nudges itself once on scroll unless touched; both are labelled illustrations, no idle loop. Every skill is a chip and nothing is selected on load; picking one shows a slim readout pinned to the bottom edge from md up (what it means, the projects that used it, links; kept to about 70 px so it never covers a figure) and opens the same detail inside its tile below md; skills that shared a project with it get a red mark. The join is made on the server from `projects.json`, so no use is claimed that the data does not show. (2) **Certifications**: a ledger grouped by year, newest first: one hairline row per year (the year in display type, pinned while its certificates scroll from md up, with a count) beside that year's tickets (narrow month stub that fills with the accent on hover, issuer, name, skills, link); rows not a fixed grid, so any number of certificates or years fills the page cleanly (the Deloitte AI Essentials certifications are deliberately not listed: the owner does not remember their names, so nothing is invented; add them to `certifications.json` if the details turn up). An earlier date rail was dropped: it was sparse and repeated the year. `proficiency` and `color` removed from `skills.json` (self-rated numbers, banned by §4.7); groups carry a plain-language `blurb`, an optional `note`/`notes` (Deloitte internship, SAP ABAP at ONGC), `about` (per-skill meaning) and `figure` | The first version gave Azure and Databricks two full-width sections, too much weight for the newest skills, and a 3-column matrix of 8 groups left one empty cell; a pinned top readout then covered the figures, so it moved to a slim bottom bar. Folding the new skills into the bento as its large tiles fixes both and cuts a client island. Figures follow the project-art language (tokens only, opacity/transform only, reduced motion = final state). Counts and "used in" links are derived, never typed |
| 2026-09-21 | Career rebuilt (`/career`): (1) **The run**: study, work, contests, projects and certificates in five lanes on one shared month axis (first month on record to the current month), so overlap is visible (the degree, the virtual Deloitte role, ONGC and three projects run at the same time). A native range input is laid over the plot with a zero-width thumb, so a pointer, touch or the arrow keys move a cursor to a month and a board lists what was going on in that month (a fixed five-cell board, never an empty layout); any bar or mark can be picked for its detail and link; once, when the chart scrolls into view, the cursor runs the whole way from the first month to now (3.2 s, skipped if focus is already in the chart); without the sweep (reduced motion, no JS) it rests on the busiest month, the latest month in which the most lanes overlap, computed from the data; an empty board cell says what came last in its lane, so it is never a bare dash; any press on the plot scrubs, including on a bar or mark (a still click on one still picks it), and the native range input underneath is keyboard-only; items going on in the cursor's month light up in the accent, so the sweep reads as a beam passing over the run; Replay runs it again and Now jumps to the current month; no sweep and no Replay under reduced motion; the board's items are buttons too, which is how a phone opens a detail. Bars are spans (foreground; the current role in the accent), single dates use shapes not colour (diamond = contest, hollow square = project, tick = certificate), marks a month or less apart stack in sub-rows so none overlap, and below md the marks are decoration and the cursor does the work (no 6 px tap targets). Every position, count and label is derived from `timeline.json`, `projects.json` and `certifications.json` in `src/lib/career.ts`; the axis ends at the current month and the page revalidates daily. (2) **Chapters**: one per place, newest first (Deloitte, Manipal, ONGC, Competitions); a place with several roles reads oldest to newest along a rail (Deloitte: virtual SAP Analyst, then onsite Data Modernization and Migration); chips for what `skills.json` says was learned there; each chapter header carries one strip of the same axis with its own span filled; the Hackfest line gets a 200-cell picture (one cell = 10 entries, 5 lit) read from its own text. (3) **Data corrected by the owner**: the ONGC numbers (8M+ data points, 40% time saved, 5+ departments) were not true and are removed everywhere including the About page; ONGC now says only that the owner helped build a small part of an oil well management system in SAP ABAP (T-codes and GUI features) and worked with and preprocessed the data; HANA DB, FAISS and PyTorch were dropped from that entry because the owner did not mention them; Deloitte is two roles at one employer; the JSON-LD lists Deloitte once | Owner: "make the career page interesting and awesome", then supplied the Deloitte timeline and corrected ONGC. The old page was a newest-first hairline list, the same texture as the rest of the site; the data's interesting fact is overlap, which a list cannot show. Five lanes on one axis fits the multi-track timelines seen in the research (career lanes, horizontal rails) without a dependency, GSAP or horizontal scroll hijack, and the cursor doubles as the accessible control |
| 2026-09-21 | GitHub rebuilt (`/github`), server-rendered from the GraphQL API and cached an hour (`revalidate = 3600`); the client fetch, React Query provider, skeleton and `/api/github` route are gone. Two requests: a small profile query (user id, `contributionYears`) then one detail query (per-year `contributionsCollection` aliases, pinned + owned public repos with per-language bytes and my commit history filtered by author id, contributed-to repos, PRs, issues). **Privacy gate is `lib/github/model.ts`**: only repos from PUBLIC connections become rows, commits, log lines or collaborations; `isPrivate` is re-checked; private work is only the `restrictedContributionsCount` figure, shown as a public/private split. Five sections: Ledger (absolute pulse strip: current streak, last push, joined, snapshot; a period picker of the rolling 12 months + each calendar year with a public/private bar; per-period readouts; a heatmap that follows the mouse and drags, with a real range input as the keyboard / touch path and a day readout listing that day's public commits), Rhythm (weekday x hour of public commits in IST, in the cell language), Log (removed the same day, see the next row), Repositories (pinned first; languages by bytes with a Jupyter caveat; per repo a language bar and a commit-tick strip on one shared time line; case-study link and fallback description from `projects.json`), With others (public repos I do not own: commits, PRs and issues). A pinned repo I do not own (CraveConnect) lives in Repositories, and With others excludes anything already listed. No stars, followers, sponsors or gists (vanity, or empty). OG image stays static. Feed failure or a missing token renders a designed 'unavailable' state, never a build failure; a transient failure at request time is rethrown so ISR keeps serving the last good page (build and dev return null instead). Revised after security, a11y, design and perf reviews: the heatmap and the rhythm grid are each ONE SVG (a path per level, `CellMap`), not ~530 divs (mobile Lighthouse 72 -> 89, TBT 680 -> 130 ms, period click 34 -> 15 ms, slider step ~200 -> 12 ms); hover previews a day and reverts on leave while press, drag or tap commits; committed changes that do not come from the slider go through one polite live region and the slider speaks its own `aria-valuetext`; the heatmap ramp is fg/45, 62, 80 + accent (>= 3:1 in both themes) and the selected cell has an offset ring; the calendar opens at its newest end with CSS only (`direction: rtl` scroller, `ltr` content), no post-hydration jump; the Ledger's tile wall became an inline pulse strip (last push, last active, joined, snapshot), one large public/private figure and a key-value list, with a one-line summary of the hovered day above the grid and a 7-day strip beside the readout; the accent is kept for the newest commit hash, the selected count and the 60-day commit ticks (language bars are neutral). Left alone on purpose (app-wide, shared): the page-header height and `.fade-up` h1 (so the LCP element is the first H2), fonts and render-blocking CSS, the header nav numeral colours, and the legacy 40px touch-target rule at <= 320px | Owner asked for the GitHub page to use everything the GraphQL API offers. Real data: 547 contributions in 12 months, 364 of them private, so the split explains the sparse public repos; stars/followers are near zero (DESIGN.md §2). Server rendering removes the skeleton and keeps the token off the client |
| 2026-09-21 | GitHub, owner revision: **no commit is ever shown as a hash plus message text** ("I don't want logs"). The Log section is gone; the day readout lists the repositories that got commits that day with a count and one square per commit; the query no longer fetches commit messages or hashes at all (only when a commit happened). **Repositories are a tile board**, not a list: a language spectrum bar on top (neutral tints, one bar), then a hairline-bordered grid of tiles (1 / 2 / 3 columns), each a whole-tile link with an accent rule that draws in on hover / focus. Every tile carries its own **month-by-month pixel calendar** (a row per year, J-D across, one square per month from the month the repo was created to now, brightness = my default-branch commits that month on one scale across all tiles, so the grid's shape is the repo's lifespan), a language bar with the top three, commits / created-to-last-push / licence, topics, and the case-study link. Sections are now 01 Ledger, 02 Rhythm, 03 Repositories, 04 With others | The owner does not want their work read as a commit log. The month calendar keeps the site's cell language and shows activity without exposing any commit text |
| 2026-09-21 | GitHub, second owner revision. **The repository board always shows two full rows** (`RepoBoard`): 4 / 3 / 2 / 1 columns from `xl` down, so at most 8 tiles on desktop, 6 on `lg`, 4 on `md`, 2 on phones, and "See more" (`aria-expanded`) reveals the rest. **No column is ever left empty**: the last visible tile stretches over the columns that would otherwise be blank (worked out from the tile count as plain `col-span` classes, so the server HTML is already right at every width). **Names are sanitised** (`lib/github/names.ts`): separators become spaces, camelCase is split, each word capitalised, known acronyms kept upper case ("Basic ML Projects", "RAG Bot", "Trace Lit"), the profile repo is "Profile README", and a repo with a case study is titled by the case study ("Crave Connect", "EcoHive"); non-owned repos carry a "By owner" tag. **`SNA_Dhruv_Gupta_229311248` is hidden everywhere** (`HIDDEN_REPOS`: not listed, counted, charted or named in the day readout). **With others is redesigned** as ledger rows in three parts: the project (title, owner tag, description), my last 12 months of commits there as a one-row strip of squares on one shared scale (from the commit-contribution days already in the profile query), and the pull requests and issues I opened (filled square = merged or closed, hollow = open); the whole row links to the repo and a vertical accent rule draws in on hover / focus | Owner asked for full rows with nothing empty, a see-more, tidy names, one repo removed (its name embeds a student ID) and a less empty With-others design |
| 2026-09-21 | **404 and Contact rebuilt** (owner: "interesting and amazing"). **404 = "Missing cell"** (`app/not-found.tsx` + `features/not-found/NotFoundMap`): the site drawn as cells. One dead cell with a 2 px accent border holds the display `404` and the failed address (`GET /path`, truncated to 80 chars, React-escaped); six live cells are the real pages (each a link, whole cell is the target). The address is read in the browser only (`useSyncExternalStore`, `null` on the server), because the 404 is prerendered; a small offline matcher (first segment: exact, alias, prefix, edit distance) inverts the closest page's cell (the dead cell stays the only accent box) and offers "Did you mean". The readable path has percent-escapes decoded and control / bidi-override characters stripped. The readout, the Closest label and the did-you-mean row have fixed heights and use fill / opacity, so nothing shifts after hydration (measured CLS 0). "Report a broken link" is a pre-filled `mailto:`. Next already adds `noindex`. **Contact** (`features/contact/*`): a status row (Availability / Reply window / My time), topic cells (native radios styled as cells; they prefill the subject only when it is empty or still holds a topic's own text, and are never sent, so the EmailJS payload is unchanged), a 20-cell segmented meter beside the character count, an error summary that takes focus on every failed submit ("There is a problem", links to each field, same wording inline), a `role=status` receipt (time in IST, topic, reply window) and a `role=alert` failure notice that keeps the text and shows the email; both scroll into view after a send, the submit button uses `aria-disabled` so focus is never lost, counters sit outside the labels, and the honeypot is `inert` + `aria-hidden`. EmailJS is imported on the first send, not with the page. Direct details: email + Copy, resume PDF, LinkedIn, GitHub, each with a "best for" line. Only `LocalClock`, `CopyEmail` and `ContactForm` are client code. Spam: an off-screen honeypot and a 1.5 s minimum fill time that answer a bot with a fake "sent" (client-side only: a script posting straight to EmailJS still gets through, so the EmailJS origin allowlist and rate limit must be switched on in its dashboard). Owner-stated copy lives in `SITE_CONFIG.contact.availability` ("Open to opportunities", reply within 2 days, "as of Sep 2026"), so it is one edit when it changes; the Deloitte offer is deliberately not on the page | Owner asked for both pages to be interesting; concept picked from three (Missing cell / Terminal lookup / Pixel 404); availability and reply-time wording given by the owner; research sources: GOV.UK error summary, W3C WAI form notifications, ARIA22, EmailJS abuse docs |
| 2026-09-21 | **Resume opens in a new tab** (both links: home hero, Contact) instead of downloading: `target="_blank" rel="noopener noreferrer"`, no `download`, arrow `↗`, "opens in a new tab" for screen readers; the PDF is a static file served as `application/pdf` with no attachment header, so browsers show it inline. **EmailJS contact template** rebuilt in the site's language: `docs/emailjs-contact-template.html` (paste into the template's code editor). Email-safe tables with inline styles; bone "print" by default and an ink "telemetry" version under `prefers-color-scheme: dark`; mono top bar, 3 px vermilion rule, the subject as an uppercase display title, a 20-cell strip with one accent cell (the site's cell), a hairline From / Reply address row, the message in a well with a vermilion left rule, a rectangular reply button, mono footer. It now shows `{{subject}}` (the old template dropped it). Hex values are deliberate here: email clients do not support CSS variables. Variables use `{{ }}` (HTML-escaped by EmailJS), never `{{{ }}}` | Owner asked for the resume to open in a tab and for a beautiful, readable email that matches the portfolio |
| 2026-09-21 | **Favicon = a pixel mascot of the owner, on a transparent background** (the old one was a generic blue `</>` glyph, a colour the palette does not have; a first `D▌` monogram was tried and replaced on the owner's note that the icon must be transparent and be a logo, mascot or design). A 14×14 pixel sprite on a 16×16 canvas: curly hair, a smile, charcoal shirt, and **glasses in the one accent** (`#ff582d`) with white lenses and dark pupils, so the accent sits on the most recognisable feature (the portrait's black shirt and glasses are the reference). It is the same cell language as the hero and the heatmap. A 1-cell sticker outline is computed around the silhouette so black hair stays legible on any tab bar; nothing else is drawn, so the icon is transparent (62 of 256 cells empty). `app/icon.svg` is the tab icon: whole-pixel cells on a 16-unit grid (crisp at 16 / 32 / 48 px, verified: no blended pixels), outline bone on dark browsers and carbon on light ones via `prefers-color-scheme`. `app/favicon.ico` (16 / 32 / 48, transparent, bone outline; Safari and old browsers cannot use the SVG) is the same mascot as fixed pixels. `app/apple-icon.png` (180 px) and `public/icons/icon-192.png`, `icon-512.png` (also the maskable icon) are the one place a background is kept, because iOS and Android composite transparent icons on black or crop them: an ink tile with the mascot as separate tiles and faint empty cells, inside the 80 % safe zone. The old `apple-icon.svg` was dead (iOS ignores SVG touch icons and Next's `apple-icon` only takes raster files), `public/favicon.ico` was a corrupt PNG (its first byte was mangled), and the `icons` block in `layout.tsx` was overridden by the file conventions, so all three were removed. `manifest.json` colours moved from `#0a0a0a` to the ink `#0c0a08` and it lists the PNG icons. Hex is deliberate: an icon file cannot read CSS variables. No animation in the favicon (a blinking tab icon would break the ≈ 5 s idle rule in §4.5) | Owner asked for a cool, interesting favicon, then for a transparent background and a logo, mascot or design; the mascot was chosen because the site's signature is already the pixel portrait of the owner, and it stays legible at 16 px |
| 2026-09-23 | **Stage 2 (docs/world-class-plan): first screen, demo, flagship doorway.** (1) `.t-display` is `clamp(4rem, min(18vw, 21svh), 17rem)` (deviation from §4.2's width-only size) and the hero portrait is capped at `calc(100svh - 13rem)`, so name, statement and Work / Resume / Contact end above the fold at 1536×674, 1366×768, 1440×900, 1920×1080, 768×1024 and 390×844 (measured with reduced motion; CTAs at 1536×674 moved from 888 px to 648 px). Mobile keeps `26vw`. (2) Home order is now Hero → Ticker → About → **[03] Try it** (`WhatArrives`: static three-panel demo; native radios, CSS `:has()` shows the chosen outcome, zero JS; without `:has()` all outcomes show, each labelled) → **[04] Selected work** (`FlagshipDoorway`, title/facts/credits from `projects.json`) → the Projects / Contact nav (moved out of About so action comes last). Sample records are shared with the case-page sketch (`lib/placement-sample.ts`); the invented "Eligible" column was dropped. (3) `app/loading.tsx` removed: its Suspense boundary made the prerendered home ship inside `<div hidden id="S:0">`, so `/` was blank with JS off (also true on the live site; other routes' `loading.tsx` still do this) | Stage 2 of the award plan: the review's first-screen fix at real laptop heights, the concept proven as a static base layer before any motion, and proof (the MUJ case study) one scroll after it. Height capping keeps the name the dominant element without letting it push the actions off short screens |
